<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Creating Dynamic Tabs in Bootstrap via Data attributes</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>

<ul class="nav nav-pills mb-3" id="pills-tab" role="tablist">
  <li class="nav-item" role="presentation">
    <button class="nav-link active" id="pills-home-tab" data-bs-toggle="pill" data-bs-target="#pills-home" type="button" role="tab" aria-controls="pills-home" aria-selected="true">Recruiters</button>
  </li>
  <li class="nav-item" role="presentation">
    <button class="nav-link" id="pills-profile-tab" data-bs-toggle="pill" data-bs-target="#pills-profile" type="button" role="tab" aria-controls="pills-profile" aria-selected="false">Students</button>
  </li>
  <li class="nav-item" role="presentation">
    <button class="nav-link" id="pills-contact-tab" data-bs-toggle="pill" data-bs-target="#pills-contact" type="button" role="tab" aria-controls="pills-contact" aria-selected="false">Bookings</button>
  </li>
</ul>
<div class="tab-content" id="pills-tabContent">
  <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">Recruiters</div>
  <div class="tab-pane fade" id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab">Students</div>
  <div class="tab-pane fade" id="pills-contact" role="tabpanel" aria-labelledby="pills-contact-tab">3</div>
</div>

<div class="m-4">
    <ul class="nav nav-tabs" id="myTab">
        <li class="nav-item">
            <a href="recruiters#index" class="nav-link active" data-bs-toggle="tab">Recruiters</a>
        </li>
        <li class="nav-item">
            <a href="#messages" class="nav-link" data-bs-toggle="tab">Students</a>
        </li>
    </ul>

    <div class="tab-content">
        <div class="tab-pane fade show active" id="home">
            <div class="main-container">
              <div class="general-index-container" data-controller="cards" data-action="click->cards#selectCard">
                <div class="general-index-title">
                  <h4>These are the recruiters participating in the speed-dating</h4>
                  <p>Click in for more detail</p>
                </div>
                <div class="general-index-card-container">
                  <% @current_campaign_recruiters.each do |recruiter| %>
                    <div class="general-index-card">
                      <div class="general-index-card-img-container">
                      <% if recruiter.company.photo.attached? %>
                        <%= cl_image_tag recruiter.company.photo.key %>
                      <% else %>
                        <%= image_tag "no_photo.png" %>
                      <% end %>
                      </div>
                        <div class="general-index-card-description-container">
                          <%= recruiter.full_name_and_company %>
                        </div>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        <div class="tab-pane fade" id="messages">

            <div class="main-container">
  <div class="general-index-container" data-controller="cards" data-action="click->cards#selectCard">
    <div class="general-index-title">
      <h4 class="mt-2">These are the students participating in the speed-dating</h4>
      <p>Click in for more detail</p>
    </div>
    <div class="general-index-card-container">
      <% @students.each do |student| %>
        <div class="general-index-card">
          <div class="general-index-card-img-container">
          <% if student.company.photo.attached? %>
            <%= cl_image_tag student.company.photo.key %>
          <% else %>
            <%= image_tag "no_photo.png" %>
          <% end %>
          </div>
            <div class="general-index-card-description-container">
              <%= student.full_name_and_company %>
            </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
        </div>
    </div>
  </div>
  </body>
  </html>

    <!--

              <% @current_campaign_recruiters.each do |recruiter| %>
              <p><%= recruiter.full_name_and_company %></p>
              <% end %>
        </div>


    <!--
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.1.1/css/all.css">
  </head>

  <body>
    <ul class="nav justify-content-center">
      <li class="nav-item">
        <a class="nav-link active" aria-current="page" href="recruiters#index">Recruiters</a>

      </li>
      <li class="nav-item">
        <a class="nav-link" href="students#index">Students</a>

      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Meetings</a>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
      </li>
    </ul>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>



<li><strong>Candidates:</strong> <%= @students.length %></li>
<li><strong>Recruiters:</strong> <%= @recruiters.length %></li>


<li><strong>Batch Number:</strong>

    <%= @campaign.batch_number %>

</li>

<li><strong>Students:</strong>
  <ul>
    <% @students.each do |student| %>
      <li><%= student.full_name_and_company %></li>
    <% end %>
  </ul>
</li>
<li><strong>Recruiters:</strong>
  <ul>
    <% @recruiters.each do |recruiter| %>
      <li><%= recruiter.full_name_and_company %></li>
    <% end %>
  </ul>
</li>

    -->
